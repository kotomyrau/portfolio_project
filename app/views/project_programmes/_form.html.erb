<%= simple_form_for model, method: verb, url: url do |f| %>
    <%= f.collection_select :project_id, @projects, :id, :title %>
    <%= f.collection_select :programme_id, @programmes, :id, :name %>

  <%= f.button :submit %>
<% end %>

<!-- #restaurant = projectprogramme
#loation = project
category = programme
 -->
